import { PaginatedCollection } from '@/types/global'
import React from 'react'

const Index = ({breeders} : {
    breeders: PaginatedCollection<App.Data.BreederData>
}) => {

  return (
    <div>{

        }</div>
  )
}

export default Index
